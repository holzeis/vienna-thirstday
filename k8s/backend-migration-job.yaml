# Run this Job once per deployment (or whenever the schema changes) to apply
# pending Drizzle migrations before the backend Deployment rolls out new pods.
#   kubectl apply -f k8s/backend-migration-job.yaml
#   kubectl wait --for=condition=complete job/backend-migrate -n vienna-thirstday --timeout=120s
#   kubectl delete job/backend-migrate -n vienna-thirstday   # then re-apply next time
apiVersion: batch/v1
kind: Job
metadata:
  name: backend-migrate
  namespace: vienna-thirstday
spec:
  backoffLimit: 2
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: ghcr.io/holzeis/vienna-thirstday-backend:latest
          command: ["node", "dist/db/migrate.js"]
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: vienna-thirstday-secrets
