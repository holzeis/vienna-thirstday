# Example Secret manifest - DO NOT commit real secrets to git.
#
# Copy this file to secret.yaml, fill in real values, and apply it, OR
# generate it directly with:
#
#   kubectl create secret generic vienna-thursday-secrets \
#     --namespace vienna-thursday \
#     --from-literal=POSTGRES_PASSWORD='use-a-strong-password' \
#     --from-literal=DATABASE_URL='postgresql://vienna:use-a-strong-password@postgres:5432/vienna_thursday' \
#     --from-literal=JWT_SECRET='generate-a-long-random-string' \
#     --from-literal=ADMIN_EMAIL='admin@yourclub.example' \
#     --from-literal=ADMIN_PASSWORD='choose-a-first-admin-password' \
#     --from-literal=ADMIN_NAME='Admin'
#
# NOTE: POSTGRES_PASSWORD and the password embedded in DATABASE_URL must match.
apiVersion: v1
kind: Secret
metadata:
  name: vienna-thursday-secrets
  namespace: vienna-thursday
type: Opaque
stringData:
  POSTGRES_PASSWORD: "change-me"
  DATABASE_URL: "postgresql://vienna:change-me@postgres:5432/vienna_thursday"
  JWT_SECRET: "generate-a-long-random-string"
  ADMIN_EMAIL: "admin@vienna-thursday.local"
  ADMIN_PASSWORD: "changeme123"
  ADMIN_NAME: "Admin"
