# dependencies
node_modules/

# build output
dist/
frontend/dist/
backend/dist/

# env files
.env
.env.local
*.env

# k8s secrets (never commit real secrets)
k8s/secret.yaml

# editor/OS
.DS_Store
*.log
npm-debug.log*

# postgres data volume if run outside docker
pgdata/

# Locally-exported legacy spreadsheet data (real historical player names) -
# not published to GitHub, including seasons committed before this rule
# existed (still present in git history and in the working tree - see the
# commit that added this line for how those were untracked).
backend/src/db/seed-data/kicken-*-import.json
