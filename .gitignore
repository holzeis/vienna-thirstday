# dependencies
node_modules/

# build output
dist/
frontend/dist/
backend/dist/

# env files
.env
.env.local
*.env

# k8s secrets (never commit real secrets)
k8s/secret.yaml

# editor/OS
.DS_Store
*.log
npm-debug.log*

# postgres data volume if run outside docker
pgdata/

# Locally-exported legacy spreadsheet data (real historical player names) -
# not published to GitHub. The seasons already committed are explicitly
# re-included below; a newly exported year stays local-only by default.
backend/src/db/seed-data/kicken-*-import.json
!backend/src/db/seed-data/kicken-2024-import.json
!backend/src/db/seed-data/kicken-2025-import.json
!backend/src/db/seed-data/kicken-2026-import.json
